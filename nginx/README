These files are mods/adds to the nginx/src/http/modules directory.
We've hacked the proxy module to support dynamic proxy.
You *must* define proxy_pass (w/bogus url) _first_, followed by daisy directives.
